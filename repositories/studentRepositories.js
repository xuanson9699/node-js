const getAllStudents = ({ pageSize, pageIndex, seacrhTerm }) => {};

const addStudent = ({ email, password, name, phoneNumber, gender, address }) => {};

export default { getAllStudents, addStudent };
